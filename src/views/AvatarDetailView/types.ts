export interface Avatar {
  id: number;
  name: string;
  slogan: string;
  services: string;
  departament: string;
  company_name: string;
  image: string;
}

export interface AvatarStore {
  id: number;
  label: string;
}
