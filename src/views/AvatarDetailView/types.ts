export interface Avatar {
  id: number;
  departament: string;
  image: string;
  name: string;
  services: string;
  slogan: string;
}

export interface AvatarStore {
  id: number;
  label: string;
}
